import { IndustrialCategory } from './industrial-category';

describe('IndustrialCategory', () => {
  it('should create an instance', () => {
    expect(new IndustrialCategory()).toBeTruthy();
  });
});
